import { ReactNode } from 'react';
export type Product = {
    
    id:number;
    // published: ReactNode;
    select: Boolean;
    orderId: string;
    duration:string;
    title: string;
    keyword: string;
    status: string;
    
  };
  